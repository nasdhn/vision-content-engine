# Video Engine Spec Artifacts

Status: specification artifacts only.

Rules:
- AI never emits arbitrary Remotion/FFmpeg code.
- renderer consumes only validated/pinned Assets and registries.
- ordinary renders do not fetch arbitrary internet URLs.
- V1 clean master is SDR BT.709.
- profile/registry changes are versioned.
- Remotion licensing must be re-verified before production implementation.
