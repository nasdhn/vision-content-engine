# Pattern Library Spec Artifacts

Status: specification artifacts only.

The seed files define the initial V1 strategic Pattern Library.

Important:
- `patternVersionId` values below are deterministic placeholder UUIDs for spec/reference only.
- Implementation bootstrap may seed these exact IDs or map them to generated canonical UUID v7 IDs, provided the mapping is explicit and the semantic content/version is preserved.
- Performance evidence is NOT embedded into immutable seed PatternVersion definitions.
