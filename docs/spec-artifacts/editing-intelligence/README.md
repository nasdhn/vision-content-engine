# Editing Intelligence Spec Artifacts

Status: specification artifacts only.

Contains:
- strict Zod-shaped reference schemas;
- seven V1 EditingProfile seed definitions.

Important:
- numeric values are initial V1 defaults and policy bounds, not universal truths;
- renderer implementation must add deterministic collision/reference/asset-duration validation;
- the implementation may tune profile values only through explicit versioned profile changes;
- these files are not active application code before implementation authorization.
