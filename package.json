{
  "name": "vision-content-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=24.21.0 <25",
    "pnpm": "10.34.5"
  },
  "scripts": {
    "prisma:format": "prisma format",
    "prisma:validate": "prisma validate",
    "prisma:generate": "prisma generate",
    "db:preview": "prisma migrate diff --from-empty --to-schema prisma/schema.prisma --script --output infra/migration-preview/initial.generated.sql",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "check:secrets": "node scripts/check-secrets.mjs",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm check:secrets && pnpm prisma:validate && pnpm test",
    "dev:api": "tsx --tsconfig tsconfig.json apps/api/src/main.ts",
    "dev:web": "pnpm --filter @vision/web dev",
    "build:web": "pnpm --filter @vision/web build",
    "local:init": "node scripts/init-local.mjs",
    "infra:up": "docker compose --env-file .env -f infra/compose.yaml up -d --wait --wait-timeout 120",
    "infra:down": "docker compose --env-file .env -f infra/compose.yaml down",
    "infra:status": "docker compose --env-file .env -f infra/compose.yaml ps",
    "test:infra": "tsx scripts/check-local-infra.ts",
    "test:postgres": "vitest run --config vitest.postgres.config.ts",
    "db:migrate:local": "tsx scripts/migrate-local.ts",
    "check:phase1": "pnpm check && pnpm test:postgres && pnpm build:web",
    "check:phase2": "pnpm check && pnpm test:postgres && pnpm build:web",
    "test:browser": "playwright test",
    "test:storage": "vitest run --config vitest.storage.config.ts",
    "check:phase3": "pnpm check && pnpm test:postgres && pnpm test:storage && pnpm build:web && pnpm test:browser",
    "check:phase4": "pnpm check && pnpm test:postgres && pnpm test:storage && pnpm build:web && pnpm test:browser",
    "check:phase5": "pnpm check && pnpm test:postgres && pnpm test:storage && pnpm exec tsx tests/render/remotion-smoke.ts && pnpm exec tsx tests/render/media-regression.ts && pnpm build:web && pnpm test:browser",
    "check:phase6": "pnpm check && pnpm test:postgres && pnpm test:storage && pnpm build:web && pnpm test:browser",
    "test:distribution:runtime": "tsx tests/runtime/distribution-bullmq-smoke.ts",
    "check:phase7": "pnpm test:infra && pnpm check && pnpm test:postgres && pnpm test:storage && pnpm test:distribution:runtime && pnpm build:web && pnpm test:browser",
    "test:analytics:runtime": "tsx tests/runtime/analytics-bullmq-smoke.ts",
    "check:phase8": "pnpm test:infra && pnpm check && pnpm test:postgres && pnpm test:storage && pnpm test:analytics:runtime && pnpm test:distribution:runtime && pnpm build:web && pnpm test:browser",
    "test:learning:runtime": "tsx tests/runtime/learning-bullmq-smoke.ts",
    "check:phase9": "pnpm check:phase8 && pnpm test:learning:runtime",
    "check:phase10a": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm check:secrets && pnpm exec vitest run tests/unit/local-session.test.ts tests/integration/auth-session-api.test.ts && pnpm test:browser tests/browser/auth-session.spec.ts",
    "check:phase10b": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm check:secrets && pnpm exec vitest run tests/unit/secret-resolver.test.ts tests/unit/local-session.test.ts tests/unit/vision-attribution-ingest.test.ts tests/unit/instagram-publisher.test.ts tests/unit/youtube-publisher.test.ts tests/unit/instagram-analytics-collector.test.ts tests/unit/youtube-analytics-collector.test.ts tests/unit/analytics-runtime.test.ts tests/integration/auth-session-api.test.ts && pnpm exec vitest run --config vitest.postgres.config.ts tests/postgres/umami-import.test.ts tests/postgres/analyst-runtime.test.ts && pnpm test:storage && pnpm test:distribution:runtime && pnpm test:browser tests/browser/product-capture.spec.ts",
    "check:phase10c": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm check:secrets && pnpm exec vitest run tests/unit/structured-logging.test.ts tests/integration/runtime-logging.test.ts"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "24.13.6",
    "dotenv": "17.4.2",
    "eslint": "10.11.0",
    "prettier": "3.9.8",
    "prisma": "7.10.0",
    "tsx": "4.23.13",
    "typescript": "5.9.3",
    "typescript-eslint": "8.70.0",
    "vite": "7.3.6",
    "vitest": "4.1.11",
    "zod": "4.6.5",
    "@playwright/test": "1.63.0",
    "bullmq": "6.3.8"
  }
}
